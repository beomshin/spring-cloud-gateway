server:
  port: 8000

spring:
  application:
    name: spring-cloud-gateway
  profiles:
    active: local
    group:
      local:
        - console-logging
      test:
        - console-logging
      prod:
        - console-logging
        - file-logging